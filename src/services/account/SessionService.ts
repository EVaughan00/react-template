export class SessionService {
    public static getSessionToken() {
        // TODO: Save session token
        return ""
    }

    public static saveSessionToken(token: string) {
        // TODO: Save session token
    }

    public static deleteSessionToken() {
        // TODO: Remove session token
    }
}