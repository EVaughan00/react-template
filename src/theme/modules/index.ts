import { globalStyles } from "./global";
import { shadowStyles } from "./shadows";
import { flexStyles } from "./spacing";

export default {
    global: globalStyles,
    shadows: shadowStyles,
    spacing: {
        flex: flexStyles
    }
};
